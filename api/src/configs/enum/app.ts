export enum NODE_ENV {
  DEVELOPMENT = 'development',
  TEST = 'test',
  PRODUCTION = 'production',
}

export enum STATUS_JOB_ENUM {
  PENDING = 'PENDING',
  SUCCESS = 'SUCCESS',
  FAIL = 'FAIL',
}

export enum JOB_NAME_ENUM {
  RESET_PASSWORD = 'SEND_RESET_PASSWORD',
}

export enum QUEUE_NAME_ENUM {
  MAIL = 'MAIL',
}

export enum RECORD_STATUS {
  TEMPORARY = 'TEMPORARY',
  SAVE = 'SAVE',
}

export enum SORT_ORDER {
  ASC = 'asc',
  DESC = 'desc',
}
